import { EstadoCuyPipe } from './estado-cuy-pipe';

describe('EstadoCuyPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCuyPipe();
    expect(pipe).toBeTruthy();
  });
});
