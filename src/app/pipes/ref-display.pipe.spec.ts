import { RefDisplayPipe } from './ref-display.pipe';

describe('RefDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new RefDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
