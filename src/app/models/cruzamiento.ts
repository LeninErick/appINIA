export interface CRUZAMIENTO {
    id?: string;
    idCuyhemb: string; // Referencia al cuy hembra
    idCuymach: string; // Referencia al cuy macho
    idPers: string;    // Referencia al personal que lo registró
    obseCruz: string;
    nombCruz: string;
}
